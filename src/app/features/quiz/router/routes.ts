import { Routes } from '@angular/router';
import { CreateQuizPageComponent } from '@create-quiz/components/create-quiz-page/create-quiz-page.component';

export const routes: Routes = [
    {
        path: '',
        component: CreateQuizPageComponent,
    },
];
